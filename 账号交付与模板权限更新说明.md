# 账号交付与模板权限更新 · 2026-09-21

## 如何使用
### 查看和快速交付账号
- 项目列表的“客户账号 / 密码”下新增“查看密码”；操作列新增“复制交付”。项目管理详情也有两个按钮。
- 一键复制内容包括：落地页完整地址、子账号后台完整地址、账号、密码。
- 仅总站可读取交付密码。密码按需解密，不放入普通项目列表、客户接口或日志；弹窗关闭、页面转后台或 60 秒后清空页面内容。若浏览器阻止直接复制，会显示可手动复制的文本框。
- 原有客户以前只有密码哈希。查看时显示“旧账号仅存哈希”；需要总站主动重置一次后，新密码才可再次查看和复制。旧账号密码不会因本次更新自动变动。
- 重置使旧密码和客户已登录会话失效，请重新交付给客户。总站登录密码不在此功能中展示。

### 给每个账号开放模板
- 开通项目时可以勾选开放模板；默认勾选现有两款（Feiyue / ReelShort、DPTV / MinuteDrama）。
- 已有项目：进入“管理 → 落地页模板 → 为此账号开放模板”，勾选后点“保存开放模板”。
- 至少开放一款。如果取消当前正在使用的模板，会自动切换到首个勾选项，保存前界面有明确提示。
- 客户后台仅展示已开放模板；直接请求未开放模板的预览或切换接口也会返回受限提示。重新开放后客户刷新即可显示。
- 已有账号升级后默认仍可用原先两款模板，互不影响。

## 部署
### Pixel 配置位置
- 总站项目管理已移除 Pixel 输入区；由子账号在自己的后台填写 Meta Pixel 数字 ID，已保存的项目配置保留。
- 域名验证仍由总站统一维护，与 Pixel ID 分开。
- 落地页现已内置 PageView 与 DownloadClick：保存有效 Pixel ID 后，新打开的实际页面自动启用；留空不加载，后台与预览不统计。下载点击不等于 APK 下载或安装完成。

### 覆盖更新
1. 使用旧版备份工具先备份数据库和完整私有数据目录，并保留代码回滚副本。
2. 上传 `总站_账号交付与模板权限补丁.zip` 到总站程序根目录（包含 app、public、runtime 的这一层），解压覆盖同名文件；不是只解压到 public。
3. PHP 需启用 OpenSSL，并支持 AES-256-GCM；安装页已增加检测。
4. 保留现有 runtime、hub.sqlite、secret.key 和 install.lock。无需重新安装、删锁或修改伪静态。首次访问自动补充两个新字段，原有账号和统计保留。
5. 新建或重置客户密码时，自动创建私有数据目录中的 credentials.key（或 HUB_DATA_DIR 指定目录）。不要删除它，交付密码解密依赖此密钥。请将密钥与数据库分开妥善备份，恢复时保持匹配。
6. 重新加载总站和子后台。静态 CSS / JS 版本号自动随内容更新。若服务器设置了强缓存或 PHP OPcache 不检查时间，请按服务器配置刷新缓存 / PHP 服务。

本补丁包含所需 app、public、bin 更新及测试，不包含任何数据库、测试凭据或密钥。
当前补丁同时包含总站「模板管理」入口，详见「模板管理更新说明.md」；本地已完成 87 项自动测试，并浏览器验证查看密码、交付文本、旧账号提示、模板开放/撤回、子后台动态显示与总站模板浏览。线上需上传覆盖后生效。
