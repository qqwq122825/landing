# 总站 Tabler UI 更新 · 2026-09-21

## 技术方案

- 总站采用 Tabler 1.5.1，官方 Bootstrap 组件负责导航、表单、表格、卡片、弹窗、Toast 与进度条。
- 使用普通 HTML、CSS、原生 JavaScript 和现有 PHP 接口。没有 Vue、Vite、node_modules 或前端编译要求。
- 组件库文件及许可证已包含在源码中，固定版本、同源加载，无外部 CDN / 字体依赖。业务 CSS 仅保留布局、项目预览尺寸及错误页样式。
- 子账号深色后台、落地页模板、Pixel 与数据结构保持原样。

## 保留功能

项目总览、搜索和状态筛选、自动开户、10 位随机账号 / 密码、密码查看与一键交付、模板管理与关联账号、全局 / 项目真实预览、逐账号模板权限、项目配置、图标上传、暂停恢复、密码重置、统计及操作日志。

交付密码仍按需获取；关闭弹窗、页面转后台或 60 秒后清空。登录、CSRF、项目权限和原有 CSP 规则保留，不增加 `unsafe-eval`。此更新不重置已有账号或安装锁。

## Git 更新

1. 先备份程序、数据库与私有密钥。备份数据库继续使用 `php bin/backup.php`，不要只复制正在运行的 SQLite 主文件。
2. 对已经正确绑定本仓库的总站程序目录执行 `git pull --ff-only`。
3. Web 根目录仍为 `public/`。无需安装 Node、执行 npm、编译或重新运行安装程序。
4. 保留 `runtime/`、`hub.sqlite`、`secret.key`、`credentials.key`、`install.lock`。本仓库仅提交 `runtime/.gitkeep`。
5. 若服务器 PHP OPcache 不检查文件更新，刷新 PHP 缓存 / 服务；浏览器重新加载页面。

## 压缩包更新

将 `总站_TablerUI更新补丁.zip` 上传到含 `app/`、`public/` 的总站程序目录，解压覆盖。补丁包含业务文件和完整本地组件库，不包含客户数据。不要只上传一个 HTML 文件，避免库文件遗漏。

## 验证范围

- PHP 与业务 JavaScript 语法检查、92 项自动测试、组件库文件校验和及纯 PHP 资源加载验证。
- 在独立测试数据目录检查登录、列表、交付弹窗和复制、模板管理、全局预览、项目预览、手机布局。
- 本地测试通过与线上部署是不同状态；Git 推送不自动更改尚未拉取更新的服务器。
