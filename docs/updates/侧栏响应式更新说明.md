# 总站侧栏响应式与折叠 · 2026-09-21

## 使用

- 总站顶部面包屑左侧新增折叠按钮，使用 Tabler 按钮及原生 folded 侧栏布局。
- 展开宽度从 256px 缩为 224px；收起后为 64px 图标栏，保留导航与首页 Logo，悬停提示名称。正文随侧栏宽度自动重新排版。
- 默认在 992–1279px 视口收起，1280px 起展开。手动选择优先，刷新与切换页面后保留；该浏览器偏好只保存布局状态，不包含账号或凭据。
- 992px 以下切换为顶部菜单按钮，点击后展开导航，选择菜单项后自动收起。手机模式不覆盖桌面选择。
- 导航采用清晰的线条图标，补充无障碍名称与展开状态。按钮支持键盘，布局存储不可用时仍可切换。
- 不修改客户后台、数据库、原账号或模板。继续使用本地资源，服务器无需构建。

## 验证与更新

- 103 项自动测试通过，包含断点、手动选择、刷新记忆、手机菜单、存储异常及原业务回归。
- 在独立临时数据目录浏览器验证 1440 / 1280 / 1024 / 390 / 320px 宽度、320px 低高度窗口、折叠后模板导航、平台说明弹窗和键盘操作；整页无横向溢出，未记录浏览器错误或警告。
- 只修改总站界面与测试、说明。按 [Git 部署指南](../../deploy/README.md) 备份并拉取更新，重新加载后台即可；保留 runtime、数据库、密钥及 install.lock，无需重装。
- 本地验证不代表线上已更新，服务器拉取后生效。

## 后续界面精简

按使用需求移除总览底部“从这里，开始一个新项目”和“最近动态”卡片，以及侧栏的平台说明入口、说明文字与弹窗。同步清理相关样式、事件和总览日志请求；左侧“操作日志”功能与已有审计记录保留。上面的 103 项及平台说明弹窗验收是侧栏改造当次记录。

此次精简通过 122 项自动测试、JavaScript 语法检查与 PHP 只读预检；独立临时数据下验证 1280px 总览、390px 手机布局与操作日志导航，整页无横向溢出，浏览器未记录错误或警告。仅本地验证，线上需拉取 Git 更新。
