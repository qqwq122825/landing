# Nginx / 宝塔伪静态。站点运行目录设为应用的 public/。
# PHP 解析与 HTTPS 在站点设置中单独配置，完整示例见 nginx.conf.example。
location / {
    try_files $uri /index.php$is_args$args;
}

# 通过统一入口执行安装向导，也适用于仅放行 index.php 的站点。
location = /install.php {
    rewrite ^ /index.php last;
}
