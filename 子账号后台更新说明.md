# 子账号后台更新 · 2026-09-21

## 更新内容
- `/p/项目编号/admin` 恢复之前版本的深色后台：980px 容器、7 项统计卡、柱状趋势、国家 / 设备分布、访问明细、Pixel、显示名 / 图标、APK 下载配置、真实模板缩略预览与确认切换。
- 总站根目录管理页继续保留现在的浅色界面。
- 新建项目自动生成 9 位随机编号、无固定前缀的 10 位随机账号和 10 位随机密码。创建成功后复制交付信息；最新账号交付补丁支持总站再次查看加密保存的密码。现有客户账号和密码保持原样；主动重置客户密码时生成 10 位新密码，账号不变。
- 下载地址默认留空，也支持清空；没有默认或备用 APK 链接。
- Pixel 区域自动显示和复制当前项目的完整落地页地址（含 `/p/项目编号`），不再只显示总站域名。Pixel ID 默认留空，与项目编号分开。
- 预览不计访问，各项目登录、配置和统计继续隔离。Facebook 根域名验证由总站统一维护，客户只配置自身 Pixel ID。

## 已部署站点覆盖方法
1. 先备份现有程序与数据库（数据库使用 `php bin/backup.php`，不要单独复制在线 WAL 数据库文件）。
2. 将 `总站_子账号后台更新补丁.zip` 上传到总站程序目录，即同时包含 `app`、`public`、`runtime` 的那一级；不是单独放入 `public` 内。
3. 解压覆盖同名文件。补丁包含：
   - `app/routes.php`
   - `app/analytics.php`
   - `app/tenant.html`
   - `public/assets/tenant.css`
   - `public/assets/tenant.js`
   - 本说明
4. 保留服务器原有 `runtime` 数据目录、数据库、密钥、会话与 `install.lock`。本次是界面更新，无需重新安装，也无需删除锁文件。
5. 重新打开子账号后台。HTML 禁用缓存，CSS / JS 自动携带内容哈希版本。如仍显示旧版，确认文件覆盖位置，并检查缓存规则是否绕过后台路径。

本次不调整伪静态、Cloudflare 或 DDoS 配置，不改变已有项目编号和账号密码。

## 本地验收
- PHP 语法检查通过；56 项自动测试通过（含账户随机生成、项目隔离、CSRF、安装锁与重装保留）。
- 浏览器已验证：随机账号登录、保存显示名 / Pixel / 下载地址、刷新持久化、清空 APK 地址、刷新保留未保存表单、模板先预览后切换、分页、7 / 30 天统计、退出登录。
- 390px 手机布局无整页横向溢出，访问表格独立横向滚动。
- 浏览器修改和分页测试在独立临时 SQLite 数据目录执行，未写入演示 / 生产数据库；测试数据与凭据不进入发布包。
- 本地完成，线上需上传覆盖后生效。
